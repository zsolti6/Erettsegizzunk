<Window 
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        x:Class="ErettsegizzunkAdmin.Windows.FeladatokKezel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ErettsegizzunkAdmin.Windows"
        mc:Ignorable="d"
        Title="Érettségizzünk"
        Width="1200" Height="800"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        MinHeight="400" MinWidth="600"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- DataGrid -->
        <DataGrid x:Name="dgFeladatAdatok" 
              Grid.Row="0" Grid.RowSpan="2"
              AutoGenerateColumns="False" Margin="16" 
              RowBackground="{DynamicResource MaterialDesignPaper}" 
              AlternatingRowBackground="{DynamicResource MaterialDesignPaperLight}" 
              VerticalGridLinesBrush="{DynamicResource MaterialDesignDivider}" 
              HorizontalGridLinesBrush="{DynamicResource MaterialDesignDivider}" 
              BorderBrush="{DynamicResource PrimaryHueMidBrush}" 
              BorderThickness="1" 
              SelectionMode="Extended" 
              Style="{DynamicResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Kijelölés" Binding="{Binding IsSelected}" />
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" />
                <DataGridTextColumn Width="300" Header="Leiras" Binding="{Binding Description}" IsReadOnly="True" />
                <DataGridTextColumn Width="300" Header="Leiras" Binding="{Binding Text}" IsReadOnly="True" />
                <DataGridTextColumn Header="Megoldasok" Binding="{Binding Answers}" IsReadOnly="True" />
                <DataGridTextColumn Header="Tantargy" Binding="{Binding SubjectName}" IsReadOnly="True" />
                <DataGridTextColumn Header="Szint" Binding="{Binding LevelName}" IsReadOnly="True" />
                <DataGridTextColumn Header="Típus" Binding="{Binding TypeName}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel x:Name="spUser"
                    Grid.Column="1"
                    Grid.Row="0">
            
                        
        </StackPanel>
        
        <!-- StackPanel for Controls -->
        <StackPanel x:Name="spMuveletek"
                    Margin="16"
                    VerticalAlignment="Top"
                    Grid.Column="1"
                    Grid.Row="1">
            <Button x:Name="btnUjAdatokExcelbol" 
                Content="Új feladatok feltöltése txt-ből" 
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" 
                Click="btnUjAdatokTxtbol_Click" />
            <Button x:Name="btnOldalKov" 
                Content="Következő oldal" 
                IsEnabled="False" 
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" 
                Click="btnOldalKov_Click" />
            <Button x:Name="btnOldalElozo" 
                Content="Előző oldal" 
                IsEnabled="False" 
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" 
                Click="btnOldalElozo_Click" />
            <CheckBox x:Name="cbSelectAll" 
                  Content="Összes kijelölése" 
                  Margin="8" 
                  Style="{DynamicResource MaterialDesignCheckBox}" 
                  Checked="cbSelectAll_Checked" 
                  Unchecked="cbSelectAll_Unchecked" />
            <Button x:Name="btnDelet" 
                Content="Törlés"
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource SecondaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" 
                Click="btnDelet_Click" />
            <Button x:Name="btnBack" 
                Content="Vissza" 
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" 
                Click="btnBack_Click" />
            <Button x:Name="btnAgain" 
                Content="Újra lekérdezés" 
                Margin="8" 
                Style="{DynamicResource MaterialDesignRaisedButton}" 
                Background="{DynamicResource PrimaryHueMidBrush}" 
                Foreground="{DynamicResource MaterialDesignPaper}" />
        </StackPanel>
    </Grid>


</Window>
