<Window 
    TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.FontWeight="Medium"
    TextElement.FontSize="14"
    FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
    x:Class="ErettsegizzunkAdmin.Windows.FelhasznalokKezel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ErettsegizzunkAdmin.Windows"
    mc:Ignorable="d"
    Title="Érettségizzünk"
    Width="1200" Height="800"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    MinHeight="400" MinWidth="600"
    WindowStyle="ToolWindow"
    ResizeMode="NoResize">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- DataGrid -->
        <DataGrid x:Name="dgAdatok" AutoGenerateColumns="False" Margin="16" Grid.Row="0" Grid.RowSpan="2"
              RowBackground="{DynamicResource MaterialDesignPaper}" 
              AlternatingRowBackground="{DynamicResource MaterialDesignPaperLight}" 
              VerticalGridLinesBrush="{DynamicResource MaterialDesignDivider}" 
              HorizontalGridLinesBrush="{DynamicResource MaterialDesignDivider}" 
              BorderBrush="{DynamicResource PrimaryHueMidBrush}" 
              BorderThickness="1" 
              SelectionMode="Extended" 
              Style="{DynamicResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Kijelölés" Binding="{Binding IsSelected}" />
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" />
                <DataGridTextColumn Header="Nev" Binding="{Binding LoginName}" IsReadOnly="True" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" IsReadOnly="True" />
                <DataGridTextColumn Header="Jogosultság" Binding="{Binding PermissionName}" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Aktív" Binding="{Binding Active}" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Feliratkozva" Binding="{Binding Newsletter}" IsReadOnly="True" />
                <DataGridTextColumn Header="Profilkép" Binding="{Binding ProfilePicturePath}" IsReadOnly="True" />
                <DataGridTextColumn Header="Feliratkozás dátum" Binding="{Binding SignupDate}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Side Panel -->
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="16" VerticalAlignment="Top" HorizontalAlignment="Right">

            <Button x:Name="btnUj" 
            Content="Új felhasználó felvitele" 
            Style="{DynamicResource MaterialDesignRaisedButton}" 
            Width="200" 
            Margin="0,0,0,20" 
            Click="btnUj_Click"/>
            
            <Button x:Name="btnTorol" 
            Content="Felhasználó törlése" 
            Style="{DynamicResource MaterialDesignRaisedButton}" 
            Background="{DynamicResource SecondaryHueMidBrush}" 
            Foreground="{DynamicResource MaterialDesignPaper}" 
            Width="200" 
            Margin="0,0,0,20" 
            Click="btnTorol_Click"/>

            <Button x:Name="btnVissza" 
            Content="Vissza" 
            Style="{DynamicResource MaterialDesignRaisedButton}" 
            Width="200" 
            Margin="0,0,0,20" 
            Click="btnVissza_Click"/>
        </StackPanel>

    </Grid>
</Window>
